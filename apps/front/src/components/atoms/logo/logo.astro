---
import {cn} from '../../../lib/cn'
import LogoIcon from '../icons/logo.astro'
interface Props{
	className?: string
}

const {className} = Astro.props
---

<a href='/' class={cn('inline-block h-14.25 w-19.25 md:h-24.25 md:w-32.5 text-black', className)}>
	<LogoIcon className='h-full w-full text-black' />
</a>
